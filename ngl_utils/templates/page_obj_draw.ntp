  uint8_t cnt = 0;

    /* Clear all screen */
    NGL_LCD_Clear_AllScreen({page_backcolor});

    /* Draw all static graphics primitive, labels, etc. */
    {primitives}

    /* Draw objects */
    {dobjects}